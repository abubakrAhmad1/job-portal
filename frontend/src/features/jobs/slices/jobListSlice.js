import { createSlice } from "@reduxjs/toolkit";


const initialvalue = {
    jobs : [],
    loading : false,
}

createSlice({
    name: 'job list',
    initialvalue,
    reducers : {
        
    }
})